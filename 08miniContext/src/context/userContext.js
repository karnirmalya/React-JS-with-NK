
import { createContext } from 'react';

//Global variable => cotext
const userContext = createContext({})

export default userContext;

//STEPS TO USE CONTEXT


// STEP-1
// create a context and store it in a variable and export that(context gives you a provider)

//STEP-2 
//create usercontextprovider file